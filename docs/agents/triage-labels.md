# Triage labels

Local default mapping for Matt's engineering skills:

| Canonical role | Proposed Linear label | Meaning |
| --- | --- | --- |
| needs-triage | needs-triage | Maintainer evaluation needed |
| needs-info | needs-info | Missing information |
| ready-for-agent | ready-for-agent | Specified and ready for agent implementation |
| ready-for-human | ready-for-human | Human implementation needed |
| wontfix | wontfix | Will not be actioned |

These are configured defaults, not a claim that labels exist in Linear. Inspect existing project labels before the first triage write; reuse equivalent labels and update this mapping instead of creating duplicates. Keep workflow status separate from triage labels.
