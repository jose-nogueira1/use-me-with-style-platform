// Deprecated: merged into ConfirmationLookup.tsx to match the Figma
// "06. Confirmation and Lookup" screen, which combines this page's content
// with the order-lookup form on one route. Kept as an empty stub (rather
// than deleted) because this file lives in a workspace folder this tool
// can't remove files from -- nothing imports it anymore.
export {};
